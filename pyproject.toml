[project]
name = "dmr-playground"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.4",
    "django-modern-rest[openapi,pydantic]>=0.5.0",
    "psycopg[binary]>=3.3.3",
]
